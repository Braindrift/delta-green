import { SectionPlaceholder } from '@/pages/sections/SectionPlaceholder';

export function WorkspaceAgentsPage() {
  return (
    <SectionPlaceholder
      title="Agents"
      subtitle="Player character roster"
      ticket="DEL-51"
      detail="Minimum-viable player-character support (CRUD + accept-invite picker)."
    />
  );
}
