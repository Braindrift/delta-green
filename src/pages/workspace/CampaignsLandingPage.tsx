import { SectionPlaceholder } from '@/pages/sections/SectionPlaceholder';

export function CampaignsLandingPage() {
  return (
    <SectionPlaceholder
      title="Campaigns"
      subtitle="Your handler groups and active operations"
      ticket="DEL-41"
      detail="Campaign list with empty state and create-campaign entry point."
    />
  );
}
