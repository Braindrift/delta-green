import { SectionPlaceholder } from '@/pages/sections/SectionPlaceholder';

export function WorkspaceNotificationsPage() {
  return (
    <SectionPlaceholder
      title="Notifications"
      subtitle="Inbox — invites and system alerts"
      ticket="DEL-50"
      detail="Notifications inbox screen with sidebar unread badge wired to real data."
    />
  );
}
